# Rifa

Página independiente de la rifa. Está separada de la app de Estafeta en la raíz del repositorio.

## Scripts

```bash
npm install
npm run dev
npm run build
```

El esquema de backend de la rifa vive en `supabase/rifa_schema.sql`.
