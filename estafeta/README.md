# Estafeta React Replica

Vite + React implementation of the public Estafeta website structure. The project uses reusable components in `src/components`, centralized content data in `src/data/siteData.js`, and local visual assets under `public/assets`.

## Scripts

```bash
npm install
npm run dev
npm run build
```

Component documentation is in `docs/components/README.md`.
